# Docker Environment Variables
# These variables are used by docker-compose.yml

# MySQL Configuration
MYSQL_DATABASE=sireenshaban
MYSQL_ROOT_PASSWORD=root_password
MYSQL_USER=laravel
MYSQL_PASSWORD=laravel_password
